---
title: "4 字符串"
date: "2025-10-25"
number: 1
categories:
  - 课程笔记
  - 数据结构与算法A
tags:
  - 数据结构与算法A
  - Notion 同步
permalink: "/notes/数据结构与算法A/4 字符串/"
banner_img: /images/banners/desert.jpg
category_bar: true
math: true
---

> 从本人 Notion 的大二上笔记同步；原始整理状态：完成。

# 基本概念
元素为字符的线性表  “c0c1…cn-1” n为串长
字符：基本单位 取值为字符集
编码为单字节 8bit  ASC2码
子串
特殊字串：空串 自身
真子串为非空非自身的子串
# 存储结构与实现
顺序存储有三种实现方式
- 用S\[0\]作为记录串长的存储单元  缺点：不能超过256
- 另辟存储地方 缺点：一般是静态给定的  不需要动态存储
- 用'\\0'标识
有一种渐变的字符串匹配/比较算法：
```c++
int strcmp_1(char *d, char *s) {
    int i;
    for (i=0;d[i]==s[i];++i ) {
        if(d[i]=='\0' && s[i]=='\0')
        return 0;            //两个字符串相等
    }
    //不等,比较第一个不同的字符
    return (d[i]-s[i])/abs(d[i]-s[i]);
}

```
这就是每次在for循环中比较是否相等 如果不等立刻脱离  然后return 0
# 模式匹配
用一个模式P去在大串T中搜索  在大文本中查找特定模式
朴素算法 KMP算法
朴素算法：穷举法
```c++
int FindPat_3(string T, string P, int startindex) {
    //g为T的游标，用模板P和T第g位置子串比较，
    //若失败则继续循环
    for (int g= startindex; g <= T.length() - P.length(); g++) {
        for (int j=0;  ((j<P.length()) && (T[g+j]==P[j])) ; j++)
           ;
        if (j == P.length())
        return  g;
    }
    return(-1);  // for结束，或startindex值过大,则匹配失败
}

```
最坏时间开销为O(mn)
## KMP算法
当在某个位置匹配时发现不一致时 要考虑的是向右移多少位  KMP可以保证是无回溯的算法
**KMP算法步骤**：
1. **预处理**：根据模式串 P 生成 `next` 数组。
2. **匹配**：
	- 初始化两个指针 `i` (指向S) 和 `j` (指向P)。
	- 遍历主串 S：
		- 如果 `S[i] == P[j]`，则 `i++`, `j++`。
		- 如果 `j == P.length`，说明匹配成功，返回 `i - j`。
		- 如果 `S[i] != P[j]`：
			- 如果 `j > 0`，则 `j = next[j-1]`。（**核心跳转**）
			- 如果 `j == 0`，则 `i++`。
**核心优势**：主串指针 `i` 永不回溯，时间复杂度为 **O(n + m)**
N(next)数组：最长公共前后缀的长度
- **前缀**：指一个字符串除了最后一个字符以外，所有以第一个字符开头的连续子串。
- **后缀**：指一个字符串除了第一个字符以外，所有以最后一个字符结尾的连续子串。
它的原理就是通过确认前后缀有相同的部分 跳过一定不匹配的部分


PPT给出的是另一个版本
其对于Next数组右移一位 最左边补-1
求N的算法是递归实现的
- n0 = -1，对于j \> 0的 nj+1 ，假定已知前一位置的特征数nj ，令 k = nj ；
- 当 k≥0 且 pj ≠ pk  时，则令 k = nk ; 让步骤2循环直到条件不满足
- nj+1 = k＋1 ；// 此时，k == -1或 pj == pk
```c++
int findNext(string P) {
    int j, k;
    int m = P.length( );              // m为模式P的长度
    int *next = new  int[m];          // 动态存储区开辟整数数组
    next[0] = -1;
    j = 0;  k = -1;
    while (j < m-1) {                 // 若写成 j < m 会越界
        while (k >= 0 && P[k] != P[j])// 若不等，采用 KMP 找首尾子串
            k = next[k];              // k 递归地向前找
        j++;   k++;
        if (P[k] == P[j])
            next[j] = next[k];        // 前面找 k 值，没有受优化的影响
        else next[j] = k;             // 取消if判断，则不优化
    }
    return next;
}
```
